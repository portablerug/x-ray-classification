# Configuration Directory (Planned)

Use this folder for versioned configuration files once implementation starts.

## Example Configuration Areas

- Data source settings
- Training hyperparameters
- Experiment metadata tags
- Evaluation thresholds

Keep secrets out of config files committed to Git.
